name: Setup

on:
    workflow_call:

jobs:
    Clone-Setup-Rust-tooling:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3
            - name: Rust tooling
              uses: dtolnay/rust-toolchain@stable
